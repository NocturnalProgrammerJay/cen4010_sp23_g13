{
  "scripts": {
    "start": "npm-run-all start:*",
    "start:install": "cd Milestone3 && npm install",
    "start:server": "cd Milestone3 && nodemon src/index.js",
    "build": "npm run build:milestone3",
    "build:milestone3": "cd Milestone3 && npm run build"
  },
  "dependencies": {
    "mysql2": "^3.2.0",
    "nodemon": "^2.0.22"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
